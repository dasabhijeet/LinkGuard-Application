info:
  name: Metrics API
  type: http

http:
  method: GET
  url: http://127.0.0.1:8000/metrics
  headers:
    - name: x-api-key
      value: linkguard-secret-key
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
